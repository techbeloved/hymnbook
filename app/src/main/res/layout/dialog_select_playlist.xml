<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingBottom="8dp"
        tools:background="#efefef">

        <TextView
            android:id="@+id/textview_dialog_select_playlist_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:paddingTop="8dp"
            android:paddingBottom="8dp"
            android:textAppearance="@style/TextAppearance.Hymnbook.Title"
            tools:text="@string/text_playlist_label" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerview_select_playlist"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            tools:listitem="@layout/item_simple_title" />

        <Button
            android:id="@+id/button_select_playlist_add_new"
            style="@style/Widget.MaterialComponents.Button.TextButton.Dialog"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:text="@string/new_playlist_text"
            android:textAllCaps="false" />

    </LinearLayout>
</layout>